/** 
 * 由 CoCo 源代码计划解包器解包
 *
 * 模块 ID：1403
 */

module.exports = function (e, t) {
  for (var n = -1, t$length = t.length, e$length = e.length; ++n < t$length;) {
    e[e$length + n] = t[n]
  }
  return e
}
